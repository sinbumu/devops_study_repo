---
- name: install ruby
  apt:
    name: ruby
    state: present
    update_cache: yes

- name: install serverspec gems
  gem:
    name: "{{ item }}"
    state: present
    user_install: no
  loop:
    - rake
    - serverspec
